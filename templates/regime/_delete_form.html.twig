

<form method="post"
      action="{{ path('regime_delete', {'idRegime': regime.idRegime}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ regime.idRegime) }}">
    <button class="btn">Delete</button>
</form>
