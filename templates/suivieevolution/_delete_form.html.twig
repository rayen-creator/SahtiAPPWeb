<form method="post" action="{{ path('app_suivieevolution_delete', {'id': suivieevolution.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivieevolution.id) }}">
    <button class="btn">Delete</button>
</form>
