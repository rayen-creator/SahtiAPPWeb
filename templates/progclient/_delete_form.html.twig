<form method="post" action="{{ path('app_progclient_delete', {'id': progclient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ progclient.id) }}">
    <button class="btn">Delete</button>
</form>
