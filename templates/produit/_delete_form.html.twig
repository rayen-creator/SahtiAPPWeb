<form method="post" action="{{ path('app_produit_delete', {'idProd': produit.idProd}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.idProd) }}">
    <button class="btn btn-primary">Delete</button>
</form>
