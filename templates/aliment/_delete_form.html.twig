<form method="post" action="{{ path('aliment_delete', {'idAliment': aliment.idAliment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aliment.idAliment) }}">
    <button class="btn">Delete</button>
</form>
